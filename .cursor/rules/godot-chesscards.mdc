---
description: Godot 4 项目 ChessCards 的通用规范与上下文，与用户协作长期维护
alwaysApply: true
---

# ChessCards — Godot 项目规则

## 游戏规则

- **规则书**：游戏逻辑与牌型、胡数、计分等以 [docs/GAME_RULES.md](docs/GAME_RULES.md) 为准（四色牌 · 十胡玩法）。

## 项目与引擎

- **项目名**: ChessCards
- **引擎**: Godot 4.6
- **渲染**: GL Compatibility（兼容性模式）
- **物理**: Jolt Physics
- **脚本语言**: GDScript

## 目录与资源

- 场景、脚本、资源按功能放在 `res://` 下（如 `scenes/`、`scripts/`、`assets/`），保持结构清晰。
- 新场景、脚本、资源命名与现有风格一致；无约定时用英文、小写+下划线。

## GDScript 约定

- 类型标注：对函数参数和返回值尽量写类型（如 `func foo(x: int) -> void`）。
- 信号与节点：在 `_ready()` 或合适生命周期里连接；避免在未就绪时访问子节点。
- 场景与脚本：优先用场景组合功能，脚本只写逻辑；可复用逻辑抽到 Autoload 或工具类。

## 与 Cursor 协作

- 本规则会随项目演进由用户提供信息后持续更新。
- 生成或修改场景（.tscn）、资源（.tres）时，保持与 Godot 编辑器可无缝编辑。
- 不确定的引擎/API 行为以 Godot 4.x 官方文档为准。

## 策划案同步（自动执行）

**每次**对以下内容做规则性修改时，**必须**同步更新 [docs/DESIGN_PLAN.md](docs/DESIGN_PLAN.md) 中对应段落，保证策划案与实现一致：

- **游戏规则 / 对局流程**（如回合、响应、摸牌出牌、胡/碰/杠/吃逻辑）→ 更新 **1.7 回合与响应流程**、**2.3 数据与规则层**、**2.5 对局场景实现要点**。
- **界面与可见性**（如谁可见、展示什么信息、布局约定）→ 更新 **1.6 对局界面与可见性规则**、**2.5 刷新与 UI**。
- **数据与状态**（如 GameState/GameRules 新增字段、新方法）→ 在 **2.3** 中补充说明。

修改代码后，在同一轮回复中完成对 DESIGN_PLAN.md 的对应编辑，不要遗漏。
